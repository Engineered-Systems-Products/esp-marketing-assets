# This repo exists to hold binary image files. core.autocrlf is true on the machine that
# maintains it, so mark them binary explicitly rather than relying on git's auto-detection -
# a byte-mangled JPEG would be served as a broken image with no obvious cause.
*.jpg   binary
*.jpeg  binary
*.png   binary
*.gif   binary
*.webp  binary
*.pdf   binary
